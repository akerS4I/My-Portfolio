import "../styles/components/Availability.scss";

const Availability = () => {
  return (
    <section id="availability" className="availability">
      <h2>Availability</h2>
      <div className="availability__container">
        <p>
          Available to start work from <strong></strong>.
        </p>
      </div>
    </section>
  );
};

export default Availability;
